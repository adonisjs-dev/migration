import TableAction from '../TableAction'

/**
 * Drop column action is an object containing information to drop a column.
 */
export default class DropColumnAction extends TableAction {
  /**
   * Column name.
   */
  public name = ''
}
