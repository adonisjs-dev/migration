import IncrementsAction from './IncrementsAction'

/**
 * Big increments action is an object containing information to create or alter a bigIncrements column.
 */
export default class BigIncrementsAction extends IncrementsAction {}
