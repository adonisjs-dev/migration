import ColumnAction from '../ColumnAction'

/**
 * Boolean action is an object containing information to create or alter a boolean column.
 */
export default class BooleanAction extends ColumnAction {}
