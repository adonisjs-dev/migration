import ColumnMethodAction from '../../ColumnMethodAction'

/**
 * Marks the column as an alter / modify, instead of the default add.
 */
export default class AlterAction extends ColumnMethodAction {}
